import { AlterBackgroundHandlerDirective } from './alter-background-handler.directive';

describe('AlterBackgroundHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterBackgroundHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
