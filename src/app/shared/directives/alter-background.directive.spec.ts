import { AlterBackgroundDirective } from './alter-background.directive';

describe('AlterBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
