import { NotAvialablePipe } from './not-avialable.pipe';

describe('NotAvialablePipe', () => {
  it('create an instance', () => {
    const pipe = new NotAvialablePipe();
    expect(pipe).toBeTruthy();
  });
});
